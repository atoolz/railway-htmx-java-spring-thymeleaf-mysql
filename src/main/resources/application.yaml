server:
  port: ${PORT:8080}

spring:
  thymeleaf:
    cache: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true

logging:
  level:
    root: INFO
